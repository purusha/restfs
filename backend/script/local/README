

add this into /etc/hosts

DOCKER_BRIDGE_DOCKER_0_IP      hbase-restfs

-----------------------------------------------------------

cleanup f(x)

$> docker rm -f $(docker ps -a -q)

$> docker rmi $(docker images -q)

-----------------------------------------------------------

$> docker-compose up

-----------------------------------------------------------

